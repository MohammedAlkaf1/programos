# سياسة الإبلاغ عن الثغرات

## نطاق التغطية

كل ما في هذا المستودع: تطبيق الويب، واجهة الأوامر `/api/command`، الواجهة
العامة `/api/v1`، الخطافات الواردة `/api/hooks/*`، لوحة المشغل، والعمال
الخلفيون.

## كيف تبلّغ

أرسل التفاصيل إلى البريد المخصص للأمن لدى المالك. لا تفتح بلاغًا عامًا
بثغرة، ولا تنشرها قبل إصلاحها.

يفيدنا في البلاغ: وصف الثغرة، خطوات إعادة إنتاجها، الأثر المحتمل، وأي
شيفرة إثبات. نرد خلال ثلاثة أيام عمل ونوافيك بحالة الإصلاح.

## ما لا يجب فعله

لا تختبر على بيانات مستأجرين حقيقيين، ولا تنفّذ هجمات حرمان الخدمة، ولا
تحاول الوصول إلى بيانات لا تملكها.

## أسرار التشغيل

لا يحتوي المستودع على أي مفاتيح. كل القيم الحساسة تُمرّر عبر متغيرات
البيئة الموثّقة في `.env.example`. إذا لاحظت مفتاحًا مسربًا في أي التزام،
أبلغ فورًا حتى يُبطل المفتاح.
